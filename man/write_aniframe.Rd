% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_aniframe.R
\name{write_aniframe}
\alias{write_aniframe}
\title{Write an \emph{aniframe} to disk}
\usage{
write_aniframe(data, filename, ...)
}
\arguments{
\item{data}{An \strong{aniframe} object (see \code{aniframe::is_aniframe()}).}

\item{filename}{Character string specifying the output file name.  The file
extension determines which writer is used.}

\item{...}{Additional arguments passed to the format‑specific writer
(\code{write_aniframe_csv()} or \code{write_aniframe_parquet()}).  Typical arguments
include \code{delim}, \code{col_names}, \code{compression}, etc., depending on the backend.}
}
\value{
The original \code{data} object (invisibly), enabling pipe‑friendly usage.
}
\description{
This high‑level wrapper writes an \strong{aniframe} object to a file in one of the
supported formats (\code{parquet}, \code{csv}, or \code{tsv}). We highly recommend using \code{parquet}
as neither \code{csv} or \code{tsv} can preserve the metadata.
}
\details{
\itemize{
\item \strong{Supported extensions} are \code{"parquet"}, \code{"csv"} and \code{"tsv"}. We highly recommend using \code{parquet}
as neither \code{csv} or \code{tsv} can preserve the metadata.
\item CSV/TSV files are written with the \emph{vroom} for fast I/O.
\item Parquet files are written with the \emph{arrow} package is installed (install‑on‑demand if missing).
}
}
\examples{
\dontrun{
## Create a small aniframe for demonstration
df <- aniframe::example_aniframe()

## Write the aniframe as CSV
write_aniframe(df, "demo.csv")

## Write the same aniframe as Parquet
write_aniframe(df, "demo.parquet")
}
}
